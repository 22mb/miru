{
  "name": "miru",
  "version": "2026.7.26",
  "description": "Inline review tool for AI-generated md/HTML — select text or click elements, leave comments, fully local.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "engines": {
    "bun": "1.3.14"
  },
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run build:front && bun run packages/cli/src/cli.ts",
    "dev:front": "bun run packages/cli/src/dev-server.ts",
    "build:front": "bun build packages/frontend/src/index.tsx --outfile packages/frontend/dist/miru.js --production && cp packages/frontend/src/miru.css packages/frontend/dist/miru.css",
    "build": "bun run build:front && bun build --compile packages/cli/src/cli.ts --outfile miru && bun run scripts/clean-build.ts",
    "bump": "bun run scripts/bump-version.ts",
    "test": "bun run --filter '*' test",
    "typecheck": "tsc -b",
    "lint": "oxlint --deny-warnings packages scripts",
    "fmt": "oxfmt packages scripts",
    "fmt:check": "oxfmt --check packages scripts",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "lefthook": "^2.1.10",
    "oxfmt": "^0.59.0",
    "oxlint": "^1.74.0",
    "typescript": "7.0.2"
  }
}
